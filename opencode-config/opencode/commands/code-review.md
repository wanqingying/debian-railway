---
description: 深度代码审查——用 codegraph + git blame + PRD 上下文审查 MR
---

使用 `code-review` skill 完成本次代码审查。

请先调用 `skill` 工具加载 id 为 `code-review` 的 skill（参数：`{ "id": "code-review" }`），然后严格按其流程执行。

待审查的内容（MR/分支/commit）：

$ARGUMENTS
