---
description: End-to-end requirement development, from clarification to release
---

Use the `req-to-ship-en` skill to drive this requirement development task.

First, call the `skill` tool to load the skill with id `req-to-ship-en` (parameters: `{ "id": "req-to-ship-en" }`), then strictly follow its workflow.

User requirement:

$ARGUMENTS
