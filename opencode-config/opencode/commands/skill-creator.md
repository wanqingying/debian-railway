---
description: 创建/改进 skill——新建 skill、编辑现有 skill、跑 eval 评测与描述优化
---

使用 `skill-creator` skill 完成本次任务。

请先调用 `skill` 工具加载 id 为 `skill-creator` 的 skill（参数：`{ "id": "skill-creator" }`），然后严格按其流程执行。

用户的任务描述：

$ARGUMENTS
